# frontend.py
import streamlit as st
import requests

st.title("Billing App")

# Streamlit UI code to display data fetched from Flask APIs
# You can use st.write(), st.table(), etc., to display data

if __name__ == '__main__':
    st.run()
